interface:
  display_name: "Bookando De"
  short_description: "Repo-specific patterns and workflows for bookando-de"
  default_prompt: "Use the bookando-de repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true